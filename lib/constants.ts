// The API category treated as the default/primary "Espaço de API" across the app
// (Sidebar default selection, default auth token slot, default category for new endpoints).
export const DEFAULT_API_SPACE = 'SSX Tracking Integration';
